#!/usr/bin/env sh
set -eu

npm run lint
npm run fmt:check
npm run typecheck

if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact
else
  echo "gitleaks is required for pre-commit secret scanning. Install it and retry." >&2
  exit 1
fi
